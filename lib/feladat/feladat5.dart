import 'package:flutter/material.dart';

class feladat5 extends StatelessWidget {
  const feladat5({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
