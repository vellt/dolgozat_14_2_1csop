import 'package:flutter/material.dart';

class feladat2 extends StatelessWidget {
  const feladat2({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Feladat1"),
      ],
    );
  }
}