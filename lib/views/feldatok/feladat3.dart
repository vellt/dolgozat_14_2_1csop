import 'package:flutter/material.dart';

class feladat3 extends StatelessWidget {
  const feladat3({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}